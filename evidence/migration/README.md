# Playwright Migration demo

This demo preserves three named Cypress scenarios in a migration manifest and implements their Playwright equivalents. The public validation checks source-to-target traceability, executes the Playwright destination and reports coverage parity for the declared sample.

The Cypress file is a sanitized legacy snapshot and is not executed in CI. The reusable assessment and migration toolkit remains private.
