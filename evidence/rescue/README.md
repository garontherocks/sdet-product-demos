# Automation Rescue demo

This public scenario deliberately introduces one slow-render run into a five-run sample. The legacy test uses a fixed delay and an immediate visibility check, producing a controlled 80% pass rate. The rescued test observes UI readiness and produces a controlled 100% pass rate against the same run profile.

The result demonstrates the evidence workflow. It is synthetic and must not be presented as a customer benchmark.
