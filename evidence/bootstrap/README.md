# Playwright Automation Bootstrap demo

This sanitized demo shows the evidence delivered by the Bootstrap service without publishing the private reusable framework.

The workflow validates one end-to-end checkout and two API scenarios. Every run records videos and publishes the Playwright HTML/JSON reports. The successful checkout also produces a stable portfolio screenshot.

Metrics represent this deterministic demo only and are not generalized customer claims.
